# 🧮 Java Calculator

A **fully functional calculator** built in **Java** with a clean, intuitive interface and essential operations, designed for quick and accurate calculations.

---

## Features

### Basic Operations
- `+` Addition  
- `-` Subtraction  
- `×` Multiplication  
- `÷` Division  
- `=` Equals  

### Advanced Functions
- `√` Square Root  
- `%` Percentage  
- `+/-` Toggle Positive/Negative  

### Utility
- `AC` All Clear – resets the calculator  

---

## How It Works

The calculator uses **Java Swing** for the GUI and is structured to handle user input efficiently.  

**Example Usage:**
1. Enter numbers using the numeric buttons.
2. Use `+`, `-`, `×`, or `÷` for operations.
3. Press `=` to see the result.
4. Use `AC` to clear the input or `+/-` to change the sign.
5. Use `sqrt` for square roots and `%` for percentages.

---

## Installation

1. Clone this repository:  
```bash
git clone https://github.com/your-username/java-calculator.git

---

Crunch numbers with ease—designed for learning, experimenting, and daily use.
